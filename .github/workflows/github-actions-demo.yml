name: Main Workflow

on:
  push:
    branches:
      - main

jobs:
  pre-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: read
        uses: ./.github/test/test_step.yml
        with:
          test_input: "holi"

  build:
    needs: [pre-test]
    runs-on: ubuntu-latest
    steps:
      - name: Build artifact
        run: echo "Run build............"

  deploy:
    needs: [build]
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to AWS
        run: echo "Deploying to AWS..."

